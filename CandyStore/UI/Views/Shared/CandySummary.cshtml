@model Domain.Entities.Candy

<div class="card">
    <h3>
        <strong>@Model.Name</strong>
        <span class="float-right badge badge-info">@Model.Price руб</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>

